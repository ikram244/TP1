# TP1 — Injection de Dépendances par Réflexion Java

## Description
Application Java démontrant l'injection de dépendances manuelle via la réflexion. Les classes sont chargées dynamiquement depuis un fichier de configuration, sans instanciation statique (`new`).

## Structure
